# מפרט: מערכת השלמת מפגשים בסדנאות

נכתב 3.9.2026. מיועד לבנייה ב-Claude Code על הריפו של מימו.
ענף פריסה: `claude/setup-new-app-dn1fD`.

---

## 1. הבעיה

סדנה = 5 מפגשים שבועיים באותו יום ושעה. אמא שמפספסת מפגש מבקשת להשלים אותו בקבוצה מקבילה.
היום זה ידני לגמרי: לחשב מתי המפגש המקביל רץ, לבדוק אם יש מקום, לרשום, לעדכן.
ככל שיש יותר אמהות זה גדל לינארית.

**שורש הבעיה במערכת:** ב-`workshop_cohorts` יש רק `start_date`, `start_time` ו-`end_date`.
חמשת המפגשים עצמם לא קיימים כרשומות, ושינויי מועד קיימים רק ביומן החיצוני.
בלי לוח מפגשים אמיתי במסד הנתונים אי אפשר לאוטומט כלום.

## 2. העיקרון

האמא לא "תופסת מקום". היא **מבקשת** השלמה. הבקשה לא נספרת בתפוסה ולא חוסמת אף נרשמת משלמת.
המערכת מחליטה סופית **24 שעות לפני המפגש**, כשכבר ידוע מי רשומה ומי הצהירה שהיא לא מגיעה.

הלולאה: כל היעדרות שמוצהרת מייצרת מקום, כל השלמה שמאושרת צורכת מקום.

## 3. הכללים (הוחלטו על ידי ברנדה)

| נושא | הכלל |
|---|---|
| מכסה | עד 2 השלמות לאמא לכל סדנה |
| טווח | אותו מספר מפגש, בשני המחזורים הבאים של אותה סדנה בלבד |
| עדיפות | נרשמות מקוריות תמיד קודמות. משלימות נכנסות רק למקום שנשאר |
| תור | לפי סדר הבקשות (FIFO), הראשונה שביקשה היא הראשונה שנכנסת |
| רגע ההכרעה | 24 שעות לפני המפגש |
| בחירת מועד | האמא בוחרת מתוך רשימת המועדים הזמינים |
| קיבולת | ברירת מחדל 8 לכל מחזור |

**דוגמה מספרית:** אמא במחזור עטופים שהתחיל 03/09 פספסה מפגש 2.
היא יכולה להשלים את מפגש 2 במחזור של 23/09 או במחזור של 15/10. לא רחוק מזה.

**איך נפתח מקום:**
- פחות מ-8 נרשמות במחזור המארח → יש מקום פנוי, המשלימה נכנסת לפיו.
- 8 נרשמות → מקום נפתח רק כשנרשמת מקורית מצהירה שהיא לא מגיעה למפגש הזה.

## 4. מודל נתונים

### 4.1 `cohort_meetings` (חדש)

```
id                uuid pk
cohort_id         uuid fk -> workshop_cohorts(id) on delete cascade
meeting_number    int not null            -- 1..5
meeting_date      date not null
start_time        time not null           -- ברירת מחדל מהמחזור, ניתן לדריסה למפגש בודד
is_cancelled      boolean default false
notes             text
created_at        timestamptz default now()
unique (cohort_id, meeting_number)
index on (meeting_date)
```

- **יצירה אוטומטית:** טריגר על insert ל-`workshop_cohorts` יוצר N שורות, שבועיות, מ-`start_date`/`start_time`.
- **N לכל סדנה:** להוסיף `workshops.meetings_count int not null default 5`.
- **הזזת מפגש:** עריכת `meeting_date` של שורה אחת. שאר המפגשים לא זזים אוטומטית (לפעמים זז רק האחרון). כפתור נפרד "הזז את כל המפגשים מכאן והלאה" לשינוי סדרתי.
- **`end_date` נגזר:** טריגר שמעדכן `workshop_cohorts.end_date = max(meeting_date)`. זה גם מתקן את תזמון שאלון המשוב שהיום תלוי בהזנה ידנית.

### 4.2 קיבולת

- `alter table workshop_cohorts alter column capacity set default 8`
- backfill: `update workshop_cohorts set capacity = 8 where capacity is null`
- להוסיף `not null`. בלי מספר כאן שום חישוב לא עובד. **כרגע רוב המחזורים עם `capacity = null`.**

### 4.3 `meeting_absences` (חדש)

```
id                  uuid pk
cohort_meeting_id   uuid fk -> cohort_meetings(id)
lead_id             uuid fk -> registration_leads(id)
created_at          timestamptz default now()
cancelled_at        timestamptz            -- אם ביטלה את ההצהרה
unique (cohort_meeting_id, lead_id) where cancelled_at is null
```

### 4.4 `makeup_requests` (חדש)

```
id                       uuid pk
lead_id                  uuid fk -> registration_leads(id)
source_cohort_meeting_id uuid fk -> cohort_meetings(id)   -- המפגש שפספסה
target_cohort_meeting_id uuid fk -> cohort_meetings(id)   -- המפגש שביקשה
status                   text  -- requested | confirmed | rejected | cancelled | attended
requested_at             timestamptz default now()        -- מפתח התור
decided_at               timestamptz
reject_reason            text
unique (lead_id, source_cohort_meeting_id) where status in ('requested','confirmed')
```

## 5. הלוגיקה

### 5.1 בדיקת זכאות (בעת יצירת בקשה)

1. למשתמשת פחות מ-2 בקשות `confirmed`/`attended` באותה סדנה.
2. `target.meeting_number = source.meeting_number`.
3. מחזור היעד הוא אחד משני המחזורים הבאים של אותה סדנה שה-`start_date` שלהם גדול מזה של המחזור שלה.
   *הערה: יש מקרים של שני מחזורים שמתחילים באותו תאריך בשעות שונות (למשל 03/09 ב-10:00 וב-11:45). הספירה היא לפי **תאריך התחלה ייחודי**, כך ששני מחזורים באותו יום נחשבים מחזור אחד ונותנים לה יותר בחירה. לשנות אם ברנדה מעדיפה אחרת.*
4. `target.meeting_date` רחוק יותר מ-24 שעות מעכשיו.
5. אין לה בקשה פתוחה אחרת על אותו מפגש מקור.

### 5.2 פונקציית ההקצאה `allocate_makeups(target_meeting_id)`

```
capacity  = cohort.capacity
attending = (מספר הנרשמות הפעילות במחזור המארח) - (מספר ההיעדרויות הפעילות למפגש הזה)
free      = capacity - attending

בקשות בסטטוס requested על המפגש הזה, ממוינות לפי requested_at עולה:
  ה-free הראשונות  -> confirmed
  השאר             -> rejected (reject_reason = 'no_space')
```

הפונקציה רצה **פעם אחת** לכל מפגש, 24 שעות לפניו. אין הקצאה מצטברת ואין drift.

**קרון:** job שעתי שאוסף מפגשים שה-`meeting_date + start_time` שלהם נמצא בין 24 ל-25 שעות מעכשיו, ומריץ עליהם את הפונקציה.

**ביטול היעדרות:** נרשמת מקורית יכולה לבטל את ההצהרה שלה עד רגע ההקצאה. אחרי ההקצאה זה סופי לכולם, כדי לא לשלוף מקום ממשלימה שכבר אושרה.

### 5.3 אחרי דחייה

בקשה שנדחתה מציעה לאמא אוטומטית את המועד הבא של אותו מפגש, אם קיים בתוך הטווח.
אם נגמר הטווח, מפנים אותה לסיכום המפגש באפליקציה.

### 5.4 התראות

- **אישור / דחייה:** מייל (Resend, כבר מוגדר) + התראה באפליקציה.
- **שים לב:** WhatsApp API לא יעבוד כאן בלי template message, כי חלון 24 השעות של Meta כמעט תמיד יהיה סגור ברגע הזה. לא לבנות על WhatsApp לאישור.
- **תוספת שמייצרת את המלאי:** בתזכורת השבועית הקיימת להוסיף שורה "לא מגיעה השבוע? סמני באפליקציה". זה מה שפותח את המקומות למשלימות.

## 6. ממשקים

### 6.1 אפליקציה (האמא)

מסך "הסדנה שלי":
- רשימת 5 המפגשים שלה עם תאריכים אמיתיים.
- ליד מפגש עתידי: "לא אוכל להגיע".
- אחרי סימון: רשימת המועדים הזמינים להשלמה של אותו מספר מפגש, עם תאריך, שעה, וכמה בקשות כבר בתור.
- סטטוס הבקשה, בניסוח כן: **"הבקשה שלך בתור, מקום 1. תשובה סופית ב-27/09"**. אף פעם לא "יש לך מקום".
- מונה מכסה: "השלמה 1 מתוך 2".

### 6.2 אדמין (ברנדה)

- טאב **"מפגשים"** בתוך מחזור: 5 תאריכים לעריכה, כפתור הזזה סדרתית. זה כל מה שהיא צריכה לנגוע בו.
- מסך **"השלמות"**: קריאה בלבד. מי ביקשה, למה, מה הסטטוס, ומי מגיעה לכל מפגש השבוע.
- **אפס קליקים בזרימה הרגילה.** אין אישור ידני, אין חריגות שמגיעות אליה, חוץ ממקרה שבו היא רוצה לפתוח מקום מעבר לקיבולת בכוונה.

## 7. סדר בנייה

1. `cohort_meetings` + טריגר יצירה + backfill למחזורים הקיימים + טריגר `end_date`.
2. Backfill קיבולת 8 + `not null`.
3. טאב "מפגשים" באדמין.
4. `meeting_absences` + סימון היעדרות באפליקציה.
5. `makeup_requests` + בדיקת זכאות + מסך בחירת מועד.
6. `allocate_makeups` + קרון שעתי + התראות.
7. מסך "השלמות" באדמין.

שלבים 1 עד 3 שווים בפני עצמם גם בלי כל השאר: הם מתקנים את `end_date`, את תזמון שאלון המשוב, ואת התזכורות.

## 8. פתוח להחלטה

1. שני מחזורים שמתחילים באותו תאריך: מחזור אחד לצורך הספירה, או שניים? (ברירת המחדל במפרט: אחד)
2. האם ברנדה רוצה אפשרות ידנית לפתוח מקום מעבר ל-8 במקרה חריג?
3. אמא שלא הצהירה ולא הגיעה: האם היא עדיין זכאית להשלמה? (ברירת המחדל במפרט: כן, המכסה נספרת על השלמות ולא על היעדרויות)
